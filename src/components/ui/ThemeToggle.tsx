'use client';

export default function ThemeToggle() {
  return (
    <div className="w-full px-3 py-2 rounded-lg text-sm border border-[var(--border)] text-[var(--nav-text)] bg-[var(--panel-hover)]">
      🎨 Thème unique
    </div>
  );
}
